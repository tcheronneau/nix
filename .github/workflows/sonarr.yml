name: Building nix packages
run-name: Building a nix package 
on: [push]
jobs:
  Testing:
    runs-on: native 
    steps:
      - run: echo "Running for package sonarr"
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: /run/current-system/sw/bin/nix-build . -A sonarr 

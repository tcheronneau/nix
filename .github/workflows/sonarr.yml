name: Building nix packages
run-name: Building a nix package 
on: [push]
env:
  PATH: "${PATH}:/run/current-system/sw/bin"
jobs:
  Testing:
    runs-on: native 
    steps:
      - run: echo "Running for package sonarr"
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "$GITHUB_SHA"
      - run: |
          ls -l 
          pwd
          git status
          git branch
          git diff-tree --no-commit-id --name-only  bae161319f5508217554df1805503fd9175a430d
          git diff-tree --no-commit-id --name-only "${GITHUB_SHA}" 
          
      #- run: nix build .#sonarr 
